{{!-- Previous/next page links - only displayed on page 2+ --}}
<!--<div class="extra-pagination inner">-->

<!--</div>-->

{{!-- This is the post loop - each post will be output using this markup --}}
<div class="mateblock post-feed">
    {{#foreach posts limit="{{@config.posts_per_page}}"}}
        <article class="block {{post_class}}">
            <a href="{{url}}">

                <div class="rela post__image">
                    <img src="{{img_url feature_image}}" />

                    <span class="type">
                    {{#primary_tag}}
                        {{name}}
                    {{/primary_tag}}
                    </span>
                </div>
                <h2 class="post-title">
                    {{title}}
                </h2>
                <span class="text"> {{excerpt words="20"}} </span>

            </a>
            <!--<div class="comme">21</div>-->
        </article>
    {{/foreach}}
</div>
<button type="button" id="load-posts" class="button more">
    показать больше
</button>

{{!-- Previous/next page links - displayed on every page --}}